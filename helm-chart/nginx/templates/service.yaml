apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  labels:
    app: nginx2
spec:
  type: {{ .Values.service.type }}
  selector:
    app: nginx2
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}